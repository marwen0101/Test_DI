pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "building .... we are here wait"
                sh "mvn test"
            }
        }
    }
}
